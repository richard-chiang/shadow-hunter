import {Area} from '../../../client/models/area';

export class HermitsCabin implements Area {
    id:string = "hermit";
    players:number[] = [];
}

export class Church implements Area {
    id:string = "church";
    players:number[] = [];
}

export class Cemetery implements Area {
    id:string = "cemetery";
    players:number[] = [];
}

export class UnderworldGate implements Area {
    id:string = "underworld";
    players:number[] = [];
}

export class WeirdWoods implements Area {
    id:string = "weird";
    players:number[] = [];
}

export class ErstwhileAltar implements Area {
    id:string = "altar";
    players:number[] = [];
}

